function plusOne(digits) {
    const new_arr = [];
    const size = digits.length;
    let carry_bit = 0;
    for(let i = size; i >= 0; --i) {
        if(digit[i] === 9) {
            carry_bit = 1;
        }
        new_arr[i] = 
    }
}
const digits = [9, 9];
